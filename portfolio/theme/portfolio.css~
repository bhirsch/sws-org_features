/* Note: Imageflow size is determined by the size of the block it lives in. If the block has no title and no set width, it won't show up. If the title is simply "Portfolio", the slideshow is as wide as the block's title. This (below) makes the block/slideshow nice and wide. */
#block-views-portfolio-block_1 
{ 
  width: 800px; 
  height: 300px;
  background: #000;  
  padding-top: 50px;
  margin-top: 20px;
  margin-left: 80px;

}
#views-slideshow-imageflow-images-1_scrollbar
{
  top: 20px;
}
